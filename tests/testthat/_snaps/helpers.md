# e_zigzag errors informatively without start or end

    Code
      e_zigzag(e, axis = "y", start = 400)
    Condition
      Error in `e_zigzag()`:
      ! must provide start and end values

---

    Code
      e_zigzag(e, axis = "y", end = 500)
    Condition
      Error in `e_zigzag()`:
      ! must provide start and end values

